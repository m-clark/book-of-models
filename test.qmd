

# testing {.unnumbered}

This is a place to try stuff out.

```{r}
reticulate::use_condaenv("book-of-models")
```


## Python

```{python}
import pandas as pd
import numpy as np
from plotnine import *


```

```{python}

df = pd.DataFrame({'x': np.random.normal(size=100),
                   'y': np.random.normal(size=100)})

(
    ggplot(df, aes(x='x', y='y')) + 
    geom_point() +
    theme_bw()
)
```


```{python}
df.head()
```

## R

```{r}
rnorm(10)
```